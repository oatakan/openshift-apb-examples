##############################################################################
## Deprovision mysql
## This role executes much of the needed functionality to deprovision an
## application using an Ansible Playbook Bundle and delete resources created
## in the sample apb generated by the provision role.
##############################################################################

- name: mysql service
  k8s_v1_service:
    name: mysql
    namespace: '{{ namespace }}'
    state: absent

- name: deployment config for mysql
  openshift_v1_deployment_config:
    name: mysql
    namespace: '{{ namespace }}'
    state: absent

- name: mysql volume claim
  k8s_v1_persistent_volume_claim:
    name: mysql
    namespace: '{{ namespace }}'
    state: absent

